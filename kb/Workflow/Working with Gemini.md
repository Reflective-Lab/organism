---
tags: [workflow, gemini]
---
# Working with Gemini

Uses native tools + shared `just` scripts.

| I want to... | Tool |
|---|---|
| Session focus | `just focus` |
| Deep research | `codebase_investigator` |
| Batch refactoring | `generalist` |
| Fix a bug | `replace`, `write_file`, `run_shell_command` |

`kb/` is the primary source of truth. Use `save_memory` for personal preferences only.

See also: [[Workflow/Daily Journey]]
